version: '3.6'

services:
  backend:
    build: backend
    image: registry.heroku.com/lantic-backend/web
    volumes:
      - ./backend:/app
    environment: 
      - MODE=dev
    ports:
      - 5000:5000

  # frontend:
  #   build: frontend
  #   image: ...
  #   volumes:
  #     - ./frontend:/app
  #   ports:
  #     - 5000:5000